# ========================================================
# VARIÁVEIS DE AMBIENTE PARA O SCRIPT-ETL-AUTOMACAO
# Copie este arquivo para .env e preencha com seus valores locais.
# O arquivo .env NÃO será enviado para o Git.
# ========================================================

# --- Configurações das APIs ---
# URL base da API da ESL Cloud
API_BASEURL=https://rodogarcia.eslcloud.com.br
# Alias temporario aceito (legado):
# API_BASE_URL=https://rodogarcia.eslcloud.com.br

# Token para a API REST (Faturas, Ocorrências)
API_REST_TOKEN=

# Token para a API GraphQL (Coletas, Fretes)
API_GRAPHQL_TOKEN=

# Token para a API Data Export (Manifestos, Localização)
API_DATAEXPORT_TOKEN=


# --- Configurações do Banco de Dados ---
# URL de conexão JDBC para o SQL Server
DB_URL=jdbc:sqlserver://localhost:1433;databaseName=esl_cloud;encrypt=false;

# Usuário do banco de dados
DB_USER=sa

# Senha do banco de dados
DB_PASSWORD=

# --- Seguranca operacional ---
# Caminho opcional do SQLite de seguranca.
# Se vazio, usa C:\ProgramData\ExtratorESL\security\users.db (com fallback local).
EXTRATOR_SECURITY_DB_PATH=

# Pepper opcional para reforcar hash de senha.
EXTRATOR_AUTH_PEPPER=

# Politica de bloqueio por tentativas invalidas.
EXTRATOR_AUTH_MAX_TENTATIVAS=3
EXTRATOR_AUTH_BLOQUEIO_MINUTOS=5

# --- Tolerancias operacionais ETL ---
# Se invalidos por entidade ficarem dentro desses limites, o status pode permanecer COMPLETO
ETL_INVALIDOS_QUANTIDADE_MAX=500
ETL_INVALIDOS_PERCENTUAL_MAX=2.5

# Se órfãos manifestos->coletas ficarem dentro desses limites, gera ALERTA não bloqueante
ETL_REFERENCIAL_MANIFESTOS_ORFAOS_QUANTIDADE_MAX=500
ETL_REFERENCIAL_MANIFESTOS_ORFAOS_PERCENTUAL_MAX=35.0


DB_BATCH_SIZE=100
DB_POOL_CONN_TIMEOUT=30000
DB_POOL_IDLE_TIMEOUT=600000
DB_POOL_MAX_LIFETIME=1800000
DB_POOL_MAX_SIZE=10
DB_POOL_MIN_IDLE=2
# Alias temporario aceito (legado):
DB_POOL_MIN_SIZE=2
