# Configuracao

- `config/.env.example`: modelo versionado para gerar o `.env` local na raiz do projeto.
- O arquivo `.env` real continua fora de versionamento e deve permanecer na raiz para compatibilidade operacional.
