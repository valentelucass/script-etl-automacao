<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Configuração de saída para console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [exec:%X{etl_execution_id:-n/a}] [cmd:%X{etl_command:-n/a}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Configuração de saída para arquivo -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/extrator-esl.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/extrator-esl.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [exec:%X{etl_execution_id:-n/a}] [cmd:%X{etl_command:-n/a}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Nível de log padrão -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
    
    <!-- Configurações específicas para pacotes -->
    <logger name="br.com.extrator" level="INFO" />
    
    <!-- Configurações de DEBUG para investigação de problemas de banco de dados -->
    <logger name="br.com.extrator.db" level="INFO" />
    <logger name="java.sql" level="WARN" />
    <logger name="org.hibernate.SQL" level="WARN" />
    <logger name="com.zaxxer.hikari" level="WARN" />
    <logger name="br.com.extrator.util.banco.GerenciadorConexao" level="WARN" />
</configuration>
