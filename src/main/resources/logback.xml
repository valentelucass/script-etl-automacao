<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Configuração de saída para console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Configuração de saída para arquivo -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/extrator-esl.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/extrator-esl.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Nível de log padrão -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
    
    <!-- Configurações específicas para pacotes -->
    <logger name="br.com.extrator" level="INFO" />
    
    <!-- Configurações de DEBUG para investigação de problemas de banco de dados -->
    <logger name="br.com.extrator.db" level="DEBUG" />
    <logger name="java.sql" level="DEBUG" />
    <logger name="org.hibernate.SQL" level="DEBUG" />
</configuration>