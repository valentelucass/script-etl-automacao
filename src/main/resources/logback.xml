<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="etlBaseDir" value="${ETL_BASE_DIR:-.}" />
    <!-- Configuração de saída para arquivo -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${etlBaseDir}/logs/aplicacao/runtime/extrator-esl.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${etlBaseDir}/logs/aplicacao/runtime/extrator-esl.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>20</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [exec:%X{etl_execution_id:-n/a}] [cmd:%X{etl_command:-n/a}] [cycle:%X{etl_cycle_id:-n/a}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Nível de log padrão -->
    <root level="INFO">
        <appender-ref ref="FILE" />
    </root>
    
    <!-- Configurações específicas para pacotes -->
    <logger name="br.com.extrator" level="INFO" />
    
    <!-- Configurações de DEBUG para investigação de problemas de banco de dados -->
    <logger name="br.com.extrator.db" level="INFO" />
    <logger name="java.sql" level="WARN" />
    <logger name="org.hibernate.SQL" level="WARN" />
    <logger name="com.zaxxer.hikari" level="WARN" />
    <logger name="br.com.extrator.util.banco.GerenciadorConexao" level="WARN" />
</configuration>
